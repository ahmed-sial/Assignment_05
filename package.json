{
  "name": "heavens-above",
  "version": "1.0.0",
  "description": "Astronomical data web application",
  "scripts": {
    "test": "echo 'Running tests...' && exit 0",
    "lint": "echo 'Linting code...' && exit 0",
    "build": "mkdir -p dist && node build.js",
    "cleanup": "echo 'Cleaning up...' && rm -rf dist/temp || true",
    "dev": "echo 'Run locally: python3 -m http.server 3000'"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  },
  "dependencies": {}
}
